#pragma once

/*
class Shader
{
public:
	enum class Type { VS, GS, FS };
	Shader(char* filename, Type type);
	~Shader();
	int compile(std::string& errorString);

	std::string filename;
	Type type;
};

*/
